using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SOMbomba : MonoBehaviour
{
    public AudioSource Sbomba;
   
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void SomBomba()
    {
        
        Sbomba.volume = 1;
    }

}
