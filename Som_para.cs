using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Som_para : MonoBehaviour
{
    //Som Botão
    public AudioSource Spara;

    // Start is called before the first frame update
    void Start()
    {
        Spara.volume = 0;
    }

    // Update is called once per frame
    void Update()
    {

    }
}